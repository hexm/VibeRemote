你是一名高级程序员。
客户想开一个功能架构与 XZB-1248/Spark 类似的项目，主要需求如下：
1. 项目分服务器端和客户端，服务器端具有WEB管理界面，实现用户登录、客户端列表查看、客户端基本信息管理等功能
2. 服务器端与客户端之间有状态检测机制
3. 服务器可以下发脚本给客户端，客户端执行脚本后，在服务器端可以看到执行结果和执行日志
4. 服务器端可以批量下发脚本，即一个脚本可以同时下发给多个客户端执行
5. 客户端支持windows操作系统和linux操作系统，客户端agent不能占用过多资源

技术需要如下：
1. 服务器端使用前后端分享开发，前端使用VUE，后端使用JAVA单体应用，不需要微服务
2. agent端尽量使用java开发，与服务器端技术一致，如果java占用资源过多，可考虑其他技术方案
3. 服务器端数据库使用mysql，不需要使用外部缓存，以减少技术复杂度


基于以上需求和思路。你准备怎么实现客户的需求，给出你的产品设计方案和技术架构设计。


我们现在要开发一个功能架构与 XZB-1248/Spark 类似的项目，主要需求如下：
1. 项目分服务器端和客户端，服务器端具有WEB管理界面，实现用户登录、客户端列表查看、客户端基本信息管理等功能
2. 服务器端与客户端之间有状态检测机制
3. 服务器可以下发脚本给客户端，客户端执行脚本后，在服务器端可以看到执行结果和执行日志
4. 服务器端可以批量下发脚本，即一个脚本可以同时下发给多个客户端执行
5. 客户端支持windows操作系统和linux操作系统，客户端agent不能占用过多资源

技术要求如下：
1. 服务器端使用前后端分享开发，前端使用VUE，后端使用JAVA单体应用，不需要微服务
2. agent端尽量使用java开发，与服务器端技术一致，如果java占用资源过多，可考虑其他技术方案
3. 服务器端数据库使用mysql，不需要使用外部缓存，以减少技术复杂度
4. 所有程序的java版本都使用jdk1.8

注意事项：
1.agent的实现可以参考demo目录下的代码
2.启动或执行脚本使用bat命令，不要使用powershell

目前项目已做了简单骨架的开发，但是还有很多需要优化，在现有代码和功能的基础上先写一份需求分析文档，文档放在
项目根目录下。

在以上需求分析文档的基础上，编写一份详细设计文档，目标是做好功能设计并分阶段实现功能，文档放在根目录下。

登录后，访问页面http://localhost:3000/agents.html
页面一片空白，控制台有报错：
Uncaught TypeError: Cannot read properties of undefined (reading 'length')


我们现在要开发一个功能架构与 XZB-1248/Spark 类似的项目，主要需求如下：
1. 项目分服务器端和客户端，服务器端具有WEB管理界面，实现用户登录、客户端列表查看、客户端基本信息管理等功能
2. 服务器端与客户端之间有状态检测机制
3. 服务器可以下发脚本给客户端，客户端执行脚本后，在服务器端可以看到执行结果和执行日志
4. 服务器端可以批量下发脚本，即一个脚本可以同时下发给多个客户端执行
5. 客户端支持windows操作系统和linux操作系统，客户端agent不能占用过多资源

技术要求如下：
1. 服务器端使用前后端分享开发，前端使用VUE，后端使用JAVA单体应用，不需要微服务
2. agent端尽量使用java开发，与服务器端技术一致，如果java占用资源过多，可考虑其他技术方案
3. 服务器端数据库使用mysql，不需要使用外部缓存，以减少技术复杂度
4. 所有程序的java版本都使用jdk1.8

注意事项：
1.agent的实现可以参考demo目录下的代码
2.启动或执行脚本使用bat命令，不要使用powershell

目前项目已做了一些开发，但是还有很多功能没有完成，在现有代码和功能的基础上：
1. 重新写需求分析文档，文档放在项目根目录下。
2. 写一份功能设计文档，文档放在项目根目录下。

--------------------------------------------------
三个程序都在开发环境运行起来了，在WEB界面下发了任务，但是状态一直为运行中 
在agent控制台看到日志：
Received task: null
这种情况不正常，请分析原因

agent重启了，但是没有接到任务，也没有执行任务
WEB界面的任务状态也没有任务变化
请分析原因并修复问题
agent日志如下：
========================================
LightScript Agent Startup Script
========================================

Enter server URL (default: http://localhost:8080):
Enter register token (default: dev-register-token):

Configuration:
Server URL: http://localhost:8080
Register Token: dev-register-token

Starting LightScript Agent...
Press Ctrl+C to stop the agent
========================================
Starting agent with UTF-8 encoding...
Starting LightScript Agent...
Server: http://localhost:8080
Register Token: dev-register-token
Registering agent...
Agent registered successfully!
Agent ID: c8d4bd73-bfc9-4c67-a5cd-6ba56d2c8d78
Agent Token: 835096a8-a174-49eb-adb2-dc6301f71931
Agent started. Waiting for tasks...
Heartbeat sent at Sat Oct 25 17:03:33 CST 2025

先解决另一个问题，为什么我每次重启agent都会注册为一个新的agent
而且有两个agent的状态都还是运行中
IP，端口，主机名都没有变化，它们应该是同一个agent

再明确一下，目前唯一定位一个agent的条件是什么？
ip, port or hostname

agent启动后没有心跳，日志如下：
Enter server URL (default: http://localhost:8080):
Enter register token (default: dev-register-token):

Configuration:
Server URL: http://localhost:8080
Register Token: dev-register-token

Starting LightScript Agent...
Press Ctrl+C to stop the agent
========================================
Starting agent with UTF-8 encoding...
Starting LightScript Agent...
Server: http://localhost:8080
Register Token: dev-register-token
Registering agent...
Agent registered successfully!
Agent ID: 0a89f109-1bb2-4e46-9f25-abbcf6220c6b
Agent Token: c73d759d-a447-4296-a879-917782c88274
Agent started. Waiting for tasks...
Heartbeat sent at Sat Oct 25 17:31:42 CST 2025
在WEB页面也看不到agent信息

agent注册的token起到什么作用，回答以下几个问题：
1.agent首次注册的token从何而来？
2.token发生变化后，双方如何交换信息？
3.如果泄露了，会有什么后果？

registerToken发生变化，双方如何共享信息？

目前在开发环境，怎么实现server重启后agent依然可以正常交互？

agentid 重启后会发生变化吗？

什么会出现两个agent，而且都是在线状态？这本来是同一个agent

不同意以上修改，agent应该轻量的，尽量不要承担过多的职能。
agent重复注册的问题应该在服务器端做好控制，不是让agent来避免

任务执行的流程是怎么样的？

如果pending的任务，下发给agent失败后，任务状态会不会回退？
下发失败能不能在服务端感知到

现在已经成功执行了一条任务，任务执行的流程是怎么样的？
agent多久拉取一次任务，每次拉取多少条任务？

服务器端能不能主动调用agent？而不是靠agent轮循来交互信息。
只分析代码，不要修改代码

方案对比
方案	实时性	复杂度	防火墙	Agent轻量	推荐度
当前轮询	❌ 5秒	✅ 简单	✅ 友好	✅ 是	⭐⭐⭐
WebSocket	✅ 毫秒	⚠️ 中等	⚠️ 一般	✅ 是	⭐⭐⭐⭐⭐
SSE	✅ 秒级	✅ 简单	✅ 友好	✅ 是	⭐⭐⭐⭐
Webhook	✅ 毫秒	✅ 简单	❌ 差	❌ 否	⭐
长轮询	✅ 秒级	✅ 简单	✅ 友好	✅ 是	⭐⭐⭐⭐

暂时不升级代码，今天到此为止，请删除多余的文件和临时文件 ，包括用于测试的脚本或文件

如果agent启动了多个进程，是否能都注册成功，在服务端是显示为一个还是多个？

怎么控制在一个机器上只能启动一个agent

agent停止后，服务端什么时候会更新状态为离线？

cd /d D:\git\gitee\lightScript
mvn  clean package -DskipTests

提交一次代码，给出命令即可，我在外部执行，不需要推送到远端

参考项目需求分析和功能设计文档
agent应该具备以下保活或自恢复能力：
1.agent启动的时候，如果服务器连接不上，应该尝试重新连接服务端，直到用户退出程序
2.在运行过程中，如果遇到网络问题或服务端重启，在环境恢复后，不需要重启agent，应该可以恢复通信和工作

在运行过程中，如果遇到网络问题或服务端重启，在环境恢复后，不需要重启agent，应该可以恢复通信和工作
目前现状是：如果服务器端重启，agent的心跳会被拒绝，返回400错误
请在服务端修复此问题

agent启动时增加重试机制没有问题，如果在运行过程中出现异常，
在服务器端如何保障恢复连接

客户端管理页面，点击查看任务按钮和下发任务的效果是一样的，查看任务应该跳转去查看当前agent的所有任务
请分析原因并优化
修改代码前先给出设计方案或思路，不用写文档

导航栏点击客户端管理 或任务管理 ，经常出现没有响应，需要点击多次才能生效，分析原因是什么，怎么优化

现在页面加载快很多了。但是点击仪表盘或客户端管理等菜单，屏幕会闪烁
怎么解决这个问题，有哪些方案？

没有达到预期的效果，页面切换的时候会出现较长时间的空白页面

任务管理页面的筛选条件需要需要agent筛选或查询能力，客户端管理页面查看任务时，跳转到任务管理页面
设置agent筛选条件即可，不需要再单独显示筛选条件，保持一致性
请按以上设计优化

批量任务，如果选择了多个agent,系统怎么处理？页面上会怎么显示？

实现方案2，具体要求包括：
1.在系统中增加批量任务的概念，批量任务可以一次选择多个agent
2.在任务管理页面增加tab页管理 ，第一个tab是普通任务，第二个tab是批量任务
3.一个批量任务可以包括多个普通任务，普通任务可以归属于某个批量任务，也可以不归属
4.普通任务和批量任务都有任务名称，页面上创建时任务名称给出默认值。创建批量任务时，普通任务
的任务名称用批量任务名称+序号
5.批量任务的状态由普通任务的状态统计出来 
6.其他功能根据项目情况确定

还有功能未实现：
1. 创建批量任务时，先创建批量任务，再根据选择的agent数量创建普通任务，批量任务和普通任务建立关联
2.批量任务列表，目标客户端改为客户端数量，点击任务数量对应的数字，链接到这个批量任务下所有任务的列表展示
3.创建批量任务和任务，都必须输入任务名称，页面上帮助填写默认值，任务名称不能重复

--------------------------------------------------
定位到WEB项目下，我希望在任务管理页面做如下优化：
1.普通任务增加展示任务名称和执行次数
2.执行失败或超时的任务，在操作栏增加重启按钮，可以再次发起任务的执行
3.任务重启后，在操作列增加历史按钮，点历史可以查看任务的所有执行历史，执行历史以窗口形式查看
在历史窗口中，每一次执行都有一行记录，包括启动时间、结束时间 、返回码、执行状态等信息，可以查看执行日志
任务历史需要增加数据库记录，需要设计好数据结构
先不要修改代码，给出你的设计方案
--------------------------------
确认以下问题：
1.任务重启后，执行次数应该是累加的，比如首次启动为1，重启一次为2，再重启一次为3
2.执行日志保存在数据库中还是用文件保存，如果日志内容全部放在数据库，出现超大的日志会不会拖累数据库
另外，用户需要下载日志如何处理？
3.写任务历史的时机是什么时候？重启的时候把上一次执行的结果写入历史，还是每当任务结束就写入？
我认为重启的时候才需要写历史，如果执行一次就成功了，那么点开任务历史应该为空
也就是说任务历史可以不包含当前执行结果

--------------------------------------------
同意调整后的方案。再次确认：
1.任务的执行次数字段是累加的，即启动了几次就显示为几；
2.修改了任务日志保存的方式，有没有其他地方需要一起调整

------------------------------
纠正设计思路：
1.任务表不需要有parentTaskId，重启任务时不需要创建新任务，重启任务的执行过程如下：
1）把当前任务执行状态写入任务历史表，任务历史表用任务ID+执行次数做唯一索引
2）设置当前任务的状态为待执行，执行次数加1，以便agent可以拉取到任务
3）agent返回执行结果，任务执行结束
2.记录执行日志时，日志文件名包括任务ID+执行次数+启动时间，方便查阅
3.任务ID中的-可以去掉，方便展示和查看

再确认以下问题：
1.写任务历史时，为什么要转存日志文件，是否因为在执行时上传的日志没有保存到文件，只保存到数据库？
2.如果是，把执行中的日志保存到数据库有没有风险？

日志要改成文件存储，但是不用区分执行中和历史，每个任务的日志统一命名，
日志文件名包括任务ID+执行次数+启动时间，方便查阅
查看历史还是查看日志，都定位到对应的日志文件即可

一步一步来，写日志的逻辑先不动。先实现重启任务的功能。
定位到WEB项目下，我希望在任务管理页面做如下优化：
1.普通任务增加展示任务名称和执行次数
2.执行失败或超时的任务，在操作栏增加重启按钮，可以再次发起任务的执行
3.任务重启后，在操作列增加历史按钮，点历史可以查看任务的所有执行历史，执行历史以窗口形式查看
在历史窗口中，每一次执行都有一行记录，包括启动时间、结束时间 、返回码、执行状态等信息，可以查看执行日志
任务历史需要增加数据库记录，需要设计好数据结构
结合之前的讨论，先给出你的设计方案

重启了开发环境，任务执行后，没有日志显示。是否需要重启agent

目前我的仓库中还有一些没有提交的文件 ，这些文件主要改动或优化了哪些功能，分析总结形成报告 

现在在macbook上面开发，把环境运行起来，测试一下功能是否正常

打开界面测试，发现两个问题：
1.任务管理中的任务执行次数显示未启动，实际应该是数字1
2.日志按钮是灰色，不能点击查看日志，修复此问题，并检查生成的日志在哪个目录下

功能优化的很好，提交一次代码，做一次代码备份

定位到LightScript目录下
当前项目作为一个开源项目，主要功能是实现远程下发任务，执行脚本，管理状态和日志
对比当前存在的类似开源项目，对于功能设计和UI设计，你作为一名资深DEVOPS开发工程师，有什么优化建议？
项目后续应该优化或新增哪些功能
给出你的思考，不要修改代码
把你的思考写到　产品方向.md中

先帮我把当前项目运行起来

前端项目的UI不好看，把前端替换成一个更好看的方案
可以采用一个比较稳定成熟又美观的前端框架

功能变更需求：
1.给前端增加一个图标吧，目前在浏览器上没有图标
2.确认客户端管理的添加客户端图标有没有用，没有用的话去掉它
3.给每个客户端增加一个状态收集的功能，点此按钮后，发起一个任务项，agent上报当前最新状态
这个功能可以复用脚本执行的能力
4.创建任务的时候，默认可选择脚本管理中存在的脚本，也可以自定义输入类型和内容
5.agent分组菜单名改为客户端分组，保持名称一致
6.还需要增加一个系统参数维护的菜单，用于配置一些系统参数

创建任务优化需求：
1.任务名称自动生成，用时间日期
2.给输入项分组，分为基本信息，执行客户端，脚本内容，执行参数四个分组
基本信息包括任务名，任务描述
输入做一下排版，不要把输入框拉的太长了

提交一次代码 

记住你的工作要求：
1.用中文交流
2.不要随便创建文档，无用的文档及时删除
3.不要随便创建脚本，无用的脚本及时删除
4.及时清理工作或测试过程中创建的临时文件
5.先说出你的实施方案，我同意后你再开始行动
6.java使用java8版本


为了完成测试
重启本地环境，包括门户，前后端，agent，服务器端使用mysql数据库
尽量调用存量脚本，不要随意新建脚本
要求本地agent在agent/localtest 目录下运行，不使用打包好的agent安装包
agent/localtest 作为agent的根目录
先停止agent进程，清空目录，然后把打包好的jar和必要的脚本复制到目录下运行


需要整体部署一次到阿里云
前端，后端，门户，agent下载包

功能优化方向：
1.前端与后端交互的日志、脚本内容需要加密
2.创建任务的时候，选择节点要带上IP，选择脚本要带上文件名
3.增加一个文件上传任务类型，支持agent上传文件到服务器指定的路径下
4.任务支持参数化，脚本内容或文件路径可以带参数
5.我想对新增任务的权限做细分，限制某些用户只能选择已有脚本创建任务
不能自定义输入脚本，以免对环境造成破坏；这样我可以创造一批测试用户，在登录页面提供测试用户，不再提供管理员用户
6.如果用户一次想选择几十台甚至几百台agent，怎么操作比较便利，功能上怎么设计最优
7.如果客户端是windows，我想增加一个功能：屏幕监控，在agent详情页面增加一个按钮入口，点此入口打开一个监控窗口，可以实时或准实时看到agent所在电脑的屏幕图像。基于现有架构实现这个功能，技术方案是什么，你先给一个方案
监控过程从agent到服务器应该是纯内存的，用户关闭了窗口就停止上传截图，除了日志外不留下任何文件或信息到磁盘。为保险起见，单次监控不能超过N分钟，N分钟后服务器主动断开连接，通知agent停止监控，释放资源。

