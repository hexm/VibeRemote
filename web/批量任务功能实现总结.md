# 批量任务功能实现总结

## ✅ 后端已完成

### 1. 实体类
- ✅ **BatchTask.java** - 批量任务实体，包含统计字段
- ✅ **Task.java** - 添加 `batchId` 和 `taskName` 字段
- ✅ **TaskSpec.java** - 添加 `taskName` 字段

### 2. Repository
- ✅ **BatchTaskRepository.java** - 批量任务数据访问
- ✅ **TaskRepository.java** - 添加批量任务相关查询方法

### 3. Service
- ✅ **BatchTaskService.java** - 批量任务业务逻辑
  - createBatchTask() - 创建批量任务
  - getBatchTasks() - 获取批量任务列表
  - getBatchTaskDetail() - 获取详情
  - getBatchTaskTasks() - 获取子任务
  - cancelBatchTask() - 取消批量任务
  - calculateBatchTaskStats() - 计算统计信息

- ✅ **TaskService.java** - 修改支持任务名称

### 4. API接口
- ✅ **WebController.java** - 添加批量任务API
  - POST `/api/web/batch-tasks/create` - 创建批量任务
  - GET `/api/web/batch-tasks` - 获取批量任务列表
  - GET `/api/web/batch-tasks/{batchId}` - 获取详情
  - GET `/api/web/batch-tasks/{batchId}/tasks` - 获取子任务
  - POST `/api/web/batch-tasks/{batchId}/cancel` - 取消批量任务

---

## ✅ 前端已完成

### 1. HTML结构（tasks.html）
- ✅ 添加Tab切换组件
- ✅ 普通任务列表和工具栏
- ✅ 批量任务列表和工具栏
- ✅ 批量任务详情对话框
- ✅ 创建任务对话框添加任务名称字段
- ✅ 批量任务对话框添加批量任务名称字段

---

## 🔄 前端需要完成（tasks.js）

### 需要添加的状态变量
```javascript
// Tab状态
const activeTab = ref('normal');

// 批量任务状态
const batchTasks = ref([]);
const batchLoading = ref(false);
const batchSearchKeyword = ref('');
const batchCurrentPage = ref(1);
const batchPageSize = ref(20);
const totalBatchTasks = ref(0);

// 批量任务详情
const showBatchDetailDialog = ref(false);
const selectedBatchTask = ref(null);
const batchTaskTasks = ref([]);
const batchTaskTasksLoading = ref(false);
```

### 需要添加的方法
```javascript
// Tab切换
const handleTabClick = (tab) => {
    if (activeTab.value === 'batch' && batchTasks.value.length === 0) {
        fetchBatchTasks();
    }
};

// 获取批量任务列表
const fetchBatchTasks = async () => {
    batchLoading.value = true;
    try {
        const response = await api.get('/web/batch-tasks', {
            params: {
                page: batchCurrentPage.value - 1,
                size: batchPageSize.value
            }
        });
        batchTasks.value = response.data.content;
        totalBatchTasks.value = response.data.totalElements;
    } catch (error) {
        ElMessage.error('获取批量任务列表失败: ' + error.message);
    } finally {
        batchLoading.value = false;
    }
};

// 查看批量任务详情
const viewBatchTaskDetail = async (batchTask) => {
    selectedBatchTask.value = batchTask;
    showBatchDetailDialog.value = true;
    
    batchTaskTasksLoading.value = true;
    try {
        const response = await api.get(`/web/batch-tasks/${batchTask.batchId}/tasks`);
        batchTaskTasks.value = response.data;
    } catch (error) {
        ElMessage.error('获取子任务失败: ' + error.message);
    } finally {
        batchTaskTasksLoading.value = false;
    }
};

// 取消批量任务
const cancelBatchTask = async (batchTask) => {
    try {
        await api.post(`/web/batch-tasks/${batchTask.batchId}/cancel`);
        ElMessage.success('批量任务已取消');
        fetchBatchTasks();
    } catch (error) {
        ElMessage.error('取消失败: ' + error.message);
    }
};

// 刷新批量任务
const refreshBatchTasks = () => {
    fetchBatchTasks();
};

// 分页处理
const handleBatchSizeChange = (val) => {
    batchPageSize.value = val;
    fetchBatchTasks();
};

const handleBatchCurrentChange = (val) => {
    batchCurrentPage.value = val;
    fetchBatchTasks();
};

// 批量任务状态文本
const getBatchStatusText = (status) => {
    const map = {
        PENDING: '等待中',
        RUNNING: '运行中',
        COMPLETED: '已完成',
        PARTIAL_FAILED: '部分失败',
        FAILED: '失败'
    };
    return map[status] || status;
};

// 批量任务状态类型
const getBatchStatusType = (status) => {
    const map = {
        PENDING: 'info',
        RUNNING: 'primary',
        COMPLETED: 'success',
        PARTIAL_FAILED: 'warning',
        FAILED: 'danger'
    };
    return map[status] || 'info';
};
```

### 需要修改的方法
```javascript
// 修改创建任务表单，添加taskName
const createTaskForm = ref({
    taskName: '',
    agentId: '',
    scriptLang: '',
    scriptContent: '',
    timeoutSec: 300
});

// 修改批量任务表单，添加batchName
const batchTaskForm = ref({
    batchName: '',
    selectedAgents: [],
    scriptLang: '',
    scriptContent: '',
    timeoutSec: 300
});

// 修改submitCreateTask，发送taskName
const submitCreateTask = async () => {
    // 给任务名称设置默认值
    if (!createTaskForm.value.taskName) {
        const agentName = getAgentName(createTaskForm.value.agentId);
        createTaskForm.value.taskName = `任务-${agentName}-${new Date().getTime()}`;
    }
    
    const taskSpec = {
        taskName: createTaskForm.value.taskName,
        scriptLang: createTaskForm.value.scriptLang,
        scriptContent: createTaskForm.value.scriptContent,
        timeoutSec: createTaskForm.value.timeoutSec
    };
    
    // ... 原有逻辑
};

// 修改submitBatchTask，调用新API
const submitBatchTask = async () => {
    // 给批量任务名称设置默认值
    if (!batchTaskForm.value.batchName) {
        batchTaskForm.value.batchName = `批量任务-${new Date().getTime()}`;
    }
    
    const params = new URLSearchParams();
    params.append('batchName', batchTaskForm.value.batchName);
    batchTaskForm.value.selectedAgents.forEach(id => params.append('agentIds', id));
    
    const taskSpec = {
        scriptLang: batchTaskForm.value.scriptLang,
        scriptContent: batchTaskForm.value.scriptContent,
        timeoutSec: batchTaskForm.value.timeoutSec
    };
    
    await api.post(`/web/batch-tasks/create?${params.toString()}`, taskSpec);
    ElMessage.success('批量任务创建成功');
    
    // 重置表单
    batchTaskForm.value = {
        batchName: '',
        selectedAgents: [],
        scriptLang: '',
        scriptContent: '',
        timeoutSec: 300
    };
    
    showBatchTaskDialog.value = false;
    
    // 切换到批量任务Tab并刷新
    activeTab.value = 'batch';
    fetchBatchTasks();
};
```

### 需要在return中暴露
```javascript
return {
    // 现有的...
    
    // 新增Tab相关
    activeTab,
    handleTabClick,
    
    // 新增批量任务相关
    batchTasks,
    batchLoading,
    batchSearchKeyword,
    batchCurrentPage,
    batchPageSize,
    totalBatchTasks,
    fetchBatchTasks,
    refreshBatchTasks,
    handleBatchSizeChange,
    handleBatchCurrentChange,
    
    // 新增批量任务详情相关
    showBatchDetailDialog,
    selectedBatchTask,
    batchTaskTasks,
    batchTaskTasksLoading,
    viewBatchTaskDetail,
    cancelBatchTask,
    
    // 新增辅助函数
    getBatchStatusText,
    getBatchStatusType,
    getAgentName: (agentId) => {
        const agent = onlineAgents.value.find(a => a.agentId === agentId);
        return agent ? agent.hostname : agentId.substring(0, 8) + '...';
    }
};
```

---

## 📋 实现步骤

1. ✅ 后端：创建实体和Repository
2. ✅ 后端：实现Service业务逻辑  
3. ✅ 后端：实现Controller API接口
4. ✅ 前端：修改HTML页面结构
5. ⏳ 前端：实现JavaScript逻辑（需要手动完成）
6. ⏳ 测试：启动服务器测试功能
7. ⏳ 数据库：需要执行建表SQL

---

## 🗄️ 数据库迁移SQL

```sql
-- 批量任务表
CREATE TABLE IF NOT EXISTS batch_tasks (
    batch_id VARCHAR(64) PRIMARY KEY,
    batch_name VARCHAR(200),
    script_lang VARCHAR(20),
    script_content TEXT,
    timeout_sec INT,
    target_agent_count INT,
    created_by VARCHAR(64),
    created_at TIMESTAMP,
    finished_at TIMESTAMP
);

-- 修改tasks表，添加字段
ALTER TABLE tasks ADD COLUMN batch_id VARCHAR(64);
ALTER TABLE tasks ADD COLUMN task_name VARCHAR(200);

-- 添加索引
CREATE INDEX idx_tasks_batch_id ON tasks(batch_id);
```

---

## 🎯 核心功能说明

### 1. 批量任务创建流程
```
用户输入批量任务名称 → 选择多个Agent → 填写脚本
  ↓
调用 POST /api/web/batch-tasks/create
  ↓
后端创建1个BatchTask记录
  ↓
后端为每个Agent创建1个Task记录
  ├─ taskName = batchName + "-" + index
  ├─ batchId = BatchTask的ID
  └─ 其他参数相同
  ↓
返回批量任务ID
```

### 2. 批量任务状态计算
```
BatchTask本身不存储状态
  ↓
查询所有子Task的状态
  ↓
统计：successTasks, failedTasks, runningTasks等
  ↓
计算综合状态和进度百分比
  ↓
实时返回给前端
```

### 3. Tab页面切换
```
普通任务Tab：
  - 显示所有batchId为null的任务
  - 工具栏：筛选、刷新、创建任务、批量任务

批量任务Tab：
  - 显示所有BatchTask记录
  - 工具栏：搜索、刷新、创建批量任务
  - 点击详情查看所有子任务
```

---

## ⚠️ 注意事项

1. **任务名称默认值**：如果用户不输入，自动生成
2. **批量任务不会被修改**：一旦创建，批量任务记录不变，只有子任务状态会更新
3. **统计信息实时计算**：每次查询批量任务时重新统计子任务状态
4. **取消操作**：取消批量任务会取消所有未完成的子任务
5. **普通任务和批量任务分离**：普通任务Tab只显示独立任务，不显示属于批量任务的子任务

---

## 🚀 下一步

1. 将上述JavaScript代码整合到 `web/js/tasks.js`
2. 重启后端服务器
3. 执行数据库迁移SQL
4. 测试功能
